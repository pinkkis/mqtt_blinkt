# MQTT Blinkt

Listen to an MQTT broker for messages to play animations on your Raspberry pi Blinkt! modules.

Uses Blinktjs. Remember to run docker container as privileged, set env variable for MQTT_BROKER_URL if not localhost.